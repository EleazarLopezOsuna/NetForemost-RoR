<nav class="navbar navbar-expand-lg text-bg-dark">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-auto mt-auto d-flex">
        <%= button_to 'Home', notes_path, method: :get, class: "btn btn-block btn-primary btn-lg" %>
      </ul>
      <%= form_with(url: search_notes_path, method: :get, class: "d-flex") do |form| %>
        <div class="form-group me-3 mt-1">
          <%= form.text_field :value, class: "form-control mt-auto mb-auto", placeholder: "Search" %>
        </div>
        <div class="form-group">
          <%= form.submit "Search", class: "btn btn-block btn-primary btn-lg"%>
        </div>
      <% end %>
    </div>
  </div>
</nav>